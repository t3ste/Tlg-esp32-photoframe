# ESP32 PhotoFrame - with optional native Telegram integration

## Changes from Upstream

This fork is ahead of [aitjcize/esp32-photoframe](https://github.com/aitjcize/esp32-photoframe)'s `main` by 80+ commits. Everything below is new in this fork, not present upstream:

**Telegram Bot integration** ([docs](TELEGRAM.md)):
- feat: native Telegram Bot rotation mode — send photos straight to the frame, alongside (not replacing) SD-card/URL rotation, with progressive-JPEG fallback through Telegram's alternate photo resolutions
- feat: Telegram image caption drawn as a text overlay on the photo
- feat: extensive Telegram command set for remote control/configuration (`/status`, `/clear`, `/restart`, `/pairing`, `/rotate_cron`, `/deep_sleep`, `/auto_rotate`, `/wake_notify`, `/error_overlay`, `/wifi_perf`, `/help`, `/list_albums`, `/active_albums`, `/enable_album`, `/clear_history`, `/rotation_notify`, `/rotation_pairing`, `/keep_originals`, `/exif_date`), plus an emergency `/telegram_reset` that clears the queue and sleeps immediately, bypassing normal processing
- feat: `/start` treated as an alias for `/help` (Telegram clients send it automatically on first open)
- feat: orientation pairing — combine two complementary portrait/landscape photos into one composed image instead of letterboxing either one (Telegram-only at first, later extended to normal auto-rotation too)
- feat: album fallback rotation for Telegram mode — falls back to normal rotation instead of leaving the previous image up indefinitely when a poll doesn't yield a new image, with an optional Telegram notification (photo re-upload) so the chat still reflects what's on the frame; itself toggleable, so the frame can instead be pinned to show only actual Telegram photos and never change on a wake with nothing new — with a second, related toggle deciding separately whether a Telegram connection failure (vs. a poll that simply found nothing new) still counts as an exception that falls back
- feat: Telegram "keep originals" toggle — archives each received photo's raw bytes (always the largest available size) to `Telegram/Originals`
- feat: streaming JPEG decoder for large Telegram document uploads that don't fit in one contiguous PSRAM buffer
- feat: experimental EXIF capture-date fallback caption when a received photo has no caption of its own
- feat: low-battery Telegram warning (below 20%, debounced per discharge cycle)
- feat: reports the actually-used weather source in `/status` (the configured provider and the one that last actually succeeded can differ)
- feat: opt-in duplicate detection via Telegram's own content-based `file_unique_id` — a re-sent/forwarded photo or file is skipped before downloading anything, no local hashing needed
- feat: EPDGZ output for composed orientation pairs, following the same on-device image format setting used for a single photo
- fix: Telegram thumbnail generation could silently overwrite/corrupt the just-downloaded original photo before it was ever converted or archived
- fix: don't reject large Telegram JPEGs before download now that the streaming decoder can actually handle them
- fix: a document upload's "saved" confirmation tried to echo back its thumbnail file as a photo, which Telegram categorically rejects — falls back to a text-only confirmation now
- feat: opt-in Telegram power-save mode — minimizes wake duration/WiFi-on time on an automatic timer wake (fewer WiFi/Telegram retries, no post-rotation config-sync window, no per-photo "saved" reply, no wake status ping, no fallback-rotation photo notification — the latter two settings are grayed out, not cleared, and resume if this is turned back off), never affecting a manual button wake so the web UI always stays reachable; a nested "latest only" sub-option processes just the newest update in a batch and permanently discards the rest (other photos, captions, and "/" commands); orientation pairing and the low-battery warning are deliberately left untouched since neither costs any extra network time
- fix: `wifi_manager_connect()` could hang indefinitely if WiFi association succeeded but DHCP then stalled (no bounded wait existed); it and its callers now always return within a definite timeout — also fixes a pre-existing bug where a *known* total connection failure still wasted up to a full 60s busy-polling a result that could never change
- fix: the Web UI's "auto-rotate orientation pairing" toggle (`/rotation_pairing`, album picks during rotation) was only shown while `rotation_mode` was set to Storage, even though it — and the rest of its settings card — apply regardless: Telegram- and URL-mode's own fallback-to-storage path calls the exact same rotation code as Storage mode's primary rotation. Now shown whenever Auto-Rotate is on, any mode; also clarified its wording (and the separate, similarly-named `/pairing` for incoming Telegram photos) in the Web UI, bot replies, and docs, since the two were easy to confuse

**Face-Aware Crop** ([docs](FACE_CROP.md)):
- feat: offline face detection in `process-cli` (BlazeFace, fully offline-capable) recommends a crop that keeps faces fully visible, saved as a versioned `<name>.facecrop.json` sidecar (39 unit tests, pure-logic coverage)
- feat: `--crop-output cropped|uncropped|both` — render the face-aware Cover crop, the uncropped Fit letterbox, or both side by side, so an album can be pre-rendered for either firmware Scale Mode setting without re-processing
- feat: `--crop-preview` — writes the full, unmodified source image with detected face boxes (blue) and the recommended crop rectangle (red) overlaid, for sanity-checking face detection/the crop heuristic before a real batch render
- fix: switched process-cli's canvas library from `canvas` to `@napi-rs/canvas` (severe Windows-only native memory leak that crashed large `--detect-faces --crop-output both` batches), which surfaced a latent Canvas-vs-ImageData detection bug in the shared rendering pipeline — both fixed
- fix: the face-crop heuristic's internal "does this face still fit" check rounded to whole pixels too early, which could reject a face that genuinely fit and needlessly shrink the recommended crop (observed cutting a 3-person photo down to 2) — now stays sub-pixel-precise internally and rounds only at the final output
- fix: the recommended crop grew only as much as the minimum needed to reach the target aspect ratio around the accepted faces, discarding image content on the sides/top/bottom even when nothing required it — now grows to the *largest* aspect-ratio box that still fits the image and contains the faces (e.g. a wide photo now only loses height, not width+height, to reach 5:3)
- fix: the face-crop heuristic applied each face's safety margin (`--face-margin`) *before* checking whether it still fit, so the margin buffer itself — not the actual faces — could push the union just over the available space and wrongly drop an otherwise-fitting face (observed excluding a 3rd person whose bare face box fit easily); margin is now applied once, after face selection, to the final accepted set
- feat: `--face-detect-tiles <n>` — additionally runs face detection on an NxN grid of overlapping tiles, to catch small/distant faces that whole-image detection misses (BlazeFace's frozen 128x128 input shrinks the whole photo regardless of resolution, so small faces can vanish before `--face-min-score` ever gets a candidate to threshold); confirmed recovering 2 faces (one sunglasses-occluded, one just small/distant) that whole-image detection missed on a real 5-person photo
- feat: firmware-side, opt-in on-demand Cover/Fit rendering — the device recognizes pre-rendered `<name>.cover.<ext>` / `<name>.fit.<ext>` variants and picks whichever matches its own Scale Mode setting, falling back to a one-time on-device render (cached afterwards) only for a genuinely still-undecoded source, verified by content, not filename
- feat: Web UI "Organize Crop Folders" maintenance action, and Cover/Fit variant pairs are deduplicated to one entry in the gallery listing and rotation loops

**Image formats & processing**:
- feat: new on-device EPDGZ encoder — Telegram-ingested photos can now be converted straight to EPDGZ (palette-indexed, gzip-compressed) instead of always PNG, matching what Web UI uploads already produced client-side; configurable per ingestion path (Telegram: device-side setting; Web UI: browser-side setting), with automatic fallback to PNG if EPDGZ can't get the memory it needs
- feat: streaming JPEG decode fallback (`tjpgd`'s low-level API driven directly) for oversized documents that don't fit the all-in-one-buffer decode path
- feat: `process-cli --split-by-orientation` — routes each album's output into `landscape`/`portrait`/`square` subfolders based on each photo's own orientation, instead of one flat folder; a photo that can't be decoded at all is copied unmodified into an `unknown/` subfolder rather than just being skipped with a console error. Off by default, folder mode only

**Weather & headline overlays** ([docs](OVERLAYS.md)):
- feat: native 3-day forecast weather overlay + RSS/Atom news headline overlay drawn on the display — no companion server or API keys needed
- feat: selectable weather data source (Open-Meteo, wttr.in, or yr.no/MET Norway)
- feat: overlay colors (black bar/white text, or inverted) and English/German condition wording, shared with Telegram caption styling
- feat: opt-in weather condition icon instead of the spelled-out word (e.g. a cloud glyph instead of "cloudy"/"bedeckt") - saves horizontal space on an already-tight line, especially with 3 forecast days. Two selectable icon sets (Settings → Overlays → "Weather condition display"): [MET Norway/yr.no's official weathericons](https://github.com/metno/weathericons) (MIT, matches the yr.no data-source option above) and a set sourced via the InkyPi project's weather plugin (individual Flaticon creators, free tier - see README.md Credits). Both are pre-rendered offline to a single fixed 24x24px size, matching Font24's text height exactly (`scripts/generate_weather_icons.py`, alpha-channel thresholded rather than dithered - these are flat/gradient-fill icons where a naive luminance threshold drops bright fill colors entirely) and mapped from the same WMO weather code this project already uses internally; works in the photo overlay bar's combined single-line and one-line-per-day layouts, and in Agenda mode's Calendar day-divider weather annotation. Icon-only representation of light/moderate/heavy intensity variants (e.g. drizzle vs. rain vs. heavy rain) is inherently hard to tell apart at this size on both sets - see the colored-icon option below for a resolution-independent way to convey that instead
- feat: opt-in traffic-light coloring for the weather icon above (Settings → Overlays, "Colored icons") on color-capable boards - green/yellow/red by severity (e.g. light/moderate/heavy rain), blue for snow, black for neutral conditions (cloudy, snow grains) - a fixed per-icon-id color table (`weather_icon_color_for_id()` in `image_processor.c`), independent of whichever icon set's own artwork colors are selected. Automatically falls back to plain black/white on grayscale-only boards, same as the Climate badges already do. Thunderstorm-with-hail needed its own icon id (even though neither icon set has a distinct hail glyph) purely so it could get its own red instead of plain thunderstorm's yellow
- feat: opt-in overlay support for already-rendered EPDGZ Storage/Auto-Rotate album images (previously PNG-only) — decodes, draws, and re-encodes the one file being shown, off by default since it's an extra step per display
- fix: the Web UI's "Display Image" gallery action never applied weather/headline overlays at all (any format, any settings) — it bypassed overlay compositing entirely, unlike the Auto-Rotate loops
- feat: opt-in low-battery corner badge — a small (~17% of panel width, not a full-width bar) "BATT NN%" indicator drawn on every display update once the battery drops below a configurable threshold (default 16%, clears 4 points above it, state remembered across deep sleep), independent of Telegram/Web UI reachability; red on color-capable panels, black on grayscale-only ones
- fix: clarified that "Also overlay pre-rendered EPDGZ images" (Settings → Weather + Headline Overlays) gates the weather/headline/battery overlays for Telegram-received photos too, not just Storage-mode albums — easy to miss since EPDGZ is the recommended default output format for both
- fix: a genuinely new Telegram photo saved as EPDGZ (the recommended default format) never got weather/headline/battery overlays at all, *even with the toggle above enabled* — its display path took a shortcut straight to the panel that bypassed overlay compositing entirely, unconditionally, regardless of any setting; only the Telegram-mode fallback-to-album-rotation path (a different code path) applied overlays correctly. Now routed through the same overlay pipeline Storage-mode's own EPDGZ album images already use
- fix: "Show capture date as caption when a photo has none" was effectively dead code for Telegram photos in normal operation — it checked EXIF at *display* time, by which point the photo had already been converted to PNG/EPDGZ (neither carries EXIF); now read from the original JPEG immediately after download, before conversion
- feat: the same capture-date caption now also applies to Storage/Auto-Rotate album images (including Telegram-mode's own fallback picture) — `process-cli` writes a new `<name>.capture.json` sidecar whenever a source photo has an EXIF capture date (the original is otherwise never kept on-device for this ingestion path, so this is the only point it can be captured at all), which the firmware reads back and draws as a caption, gated on the same setting. Not yet supported for Web UI album uploads (converted entirely client-side, no EXIF extraction there)

**Web UI**:
- feat: display history with a reset button (Settings → Auto Rotate) — random rotation cycles through every image once before repeating, persisted across reboots
- feat: toggleable thumbnail loading in the gallery (was unconditionally slowing down the HTTP server)
- feat: battery history tab (hand-rolled SVG chart) with an estimated days-remaining-until-20% figure, plus a reset button
- feat: real top-level tabs (Gallery / Settings / Battery History / Updates) instead of one long stacked page
- feat: on-display error banner, testable on demand via a Web UI button
- feat: WiFi TX-power cap for Waveshare PhotoPainter battery-brownout mitigation, now user-toggleable (default on)
- feat: opt-in checkbox to include credentials and ToDo/Calendar URLs (Telegram bot token, AI API keys, access token, custom auth header, plus the write-only-at-the-device-level ToDo/Calendar URLs, which can carry an embedded credential like a Google Calendar "secret address") in an exported config backup, via a new `GET /api/config/urls` endpoint that's only ever called when this checkbox is checked — off by default since an export is a plaintext JSON file; WiFi password can never be included since the device never returns it at all
- fix: three Settings-panel actions (save settings, save palette, factory reset) referenced the wrong variable name in their error handler, throwing an unhandled `ReferenceError` on any real failure instead of surfacing it
- fix: the gallery's per-thumbnail delete button only appeared in an unmarked 48px hover hotspot in the top-right corner — undiscoverable on desktop, never visible at all on touch screens; now reveals on hovering/focusing the whole thumbnail, and stays visible (larger, 92% opacity) on touch devices
- fix: the Settings-save success confirmation next to the Save button disappeared after 3 seconds, often too quickly to read — doubled to 6 seconds
- fix: the Settings timezone field only ever understood a plain `UTC±H[:MM]` string — loading a DST-aware POSIX TZ (e.g. `CET-1CEST,M3.5.0/2,M10.5.0/3`) silently parsed to an offset of 0, and saving *any* setting afterward (not just an edit to timezone itself) would then overwrite the device's real, working DST-aware timezone with `UTC0`; the device's own time-keeping was never at fault (it already applies whatever string it's given via `setenv("TZ", ...)` verbatim, DST rules included). The raw POSIX string is now the only source of truth end-to-end (a combobox with presets + free text replaces the lossy numeric-offset field), and the Device Time display now uses `/api/time`'s already-localized wall-clock string directly instead of reconstructing local time from a guessed offset
- fix: that same new timezone combobox could silently fail to apply a selected *preset* (typing a plain string like `UTC2` worked fine) — Vuetify's `v-combobox` inconsistently emits the whole `{title, value}` object instead of just the string on selection, which the backend's `cJSON_IsString()` check then silently rejected; a normalizing computed now unwraps either shape before it reaches the store
- fix: enabling "Show thumbnails" on an album with hundreds of images could make the entire Web UI unreachable for a long time, not just that request — `album_images_handler()` did a `stat()` syscall per image to check for a thumbnail file, and since `esp_http_server` processes requests on a single task, hundreds of sequential storage lookups inside one handler call blocked every other request for as long as the scan ran; now does one `readdir()` pass to collect the album's thumbnail filenames in memory and checks existence via string compare instead. The gallery grid also no longer renders every image in an album at once (each with its own thumbnail fetch) — a bounded first batch plus a "Load more" button caps how many concurrent thumbnail requests one album switch can generate regardless of album size
- feat: the Battery/Climate History charts' x-axis ticks show only a date (e.g. "Sep 16"), which is useless once all the visible history fits inside a single day — ticks now show a time (or day+time) instead once the data's actual time span is short enough that a bare date would repeat across every tick

**Stability fixes**:
- fix: resolve battery-wake stability issues on Waveshare PhotoPainter — a dynamic-frequency-scaling/WiFi-interrupt race and a main-task stack overflow, both confirmed via on-device coredump
- fix: WiFi TX-power cap was gated on USB being disconnected, backwards from the actual Waveshare PMIC brownout condition (USB **and** battery connected together) — now gated on battery presence instead
- fix: several task stack-overflow crashes in the Telegram/rotation pipeline (button task, deep-sleep wake, HTTP `/api/rotate`), each confirmed via live coredump and moved off the shared main-task stack where possible
- fix: `album_manager_delete_album()` failed to delete an album containing a subdirectory
- fix: `build.py` couldn't find `idf.py` on a standard Windows ESP-IDF PowerShell install; later hardened further to also prefer the real `idf.py.exe` wrapper newer installers put on PATH and to target the ESP-IDF venv's own interpreter (`IDF_PYTHON_ENV_PATH`) explicitly, and `CMakeLists.txt`'s own `python3` calls (version/partition-table generation) switched to the interpreter `idf.py` already passes via `-DPYTHON=...` — a stock Windows + official installer setup with no separate `python3` shim on PATH would otherwise silently resolve it to the Microsoft Store stub, generating no `partitions.csv` and falling back to a `dev-unknown` version, with no build error at all
- fix: the WiFi captive-portal setup page's network scan could return zero SSIDs — initially mitigated with a settle delay plus an automatic retry on the setup page, which only masked the real cause by lucky timing; live log analysis later found the actual bug: switching AP-only → APSTA to scan fires `WIFI_EVENT_STA_START`, whose handler unconditionally calls `esp_wifi_connect()` - if a wifi_config was left set from an earlier connection attempt (e.g. the setup page's own "test this SSID/password" call), that auto-triggered a real association attempt that blocked the scan outright (`ESP_ERR_WIFI_STATE`, "STA is connecting, scan are not allowed"). Now explicitly disconnects right after the mode switch to cancel that before scanning
- fix: unsynchronized concurrent access to shared in-memory state in the display-history, per-album-enabled, and OTA periodic-check managers, each now mutex-protected (or, for the album-list read path, snapshotted under a lock before parsing) against a genuine race between an HTTP request and a background task
- security: `agenda_todo_url` was logged/persisted in a way that could leak it in plaintext; config-exporting a device now also strips the Telegram bot token, AI API keys, access token, and custom auth header in addition to the fields already excluded (WiFi password, Calendar/ToDo URLs), unless the new opt-in checkbox above is used
- fix (process-cli): the `/image` endpoint accepted client-supplied dimension headers without an upper bound, unlike the already-clamped `/thumbnail` endpoint — now clamped identically to prevent an oversized native canvas allocation
- fix (process-cli): the face-crop engine's final pixel-rounding step could push a crop rectangle up to 1px past the source image's edge after independently rounding x/y/w/h — now re-clamped against the image bounds after rounding
- fix: a single failed WiFi connection attempt during the always-on cold-boot path immediately erased the saved SSID/password and restarted into captive-portal provisioning - even for a merely transient failure (router mid-reboot, brief congestion), not just genuinely wrong credentials. Now retries the connection up to 3 times before giving up, unless the AP's own disconnect reason explicitly confirms rejected credentials (a failed 4-way handshake, MIC failure, or an explicit auth-fail), which still clears after one attempt exactly as before - a real rejection can't un-happen on a retry with the same password
- fix: the in-memory "current displayed image" path buffer was only 64 bytes - too small for a real absolute path (album subdirectory + a long filename, e.g. Google Pixel Motion Photos' `<timestamp>.RAW-01.MP.COVER.epdgz`), confirmed live silently truncating one such path and making the on-display error-overlay feature fail to open it, falling back to a blank canvas instead of overlaying onto the actual photo. A second, local copy of the same path (used only to detect whether a Telegram fallback-rotation actually changed the display) had the identical risk and could misjudge whether the display changed at all, purely from mismatched truncation. Both resized to 256 bytes, matching this project's own established convention for path buffers elsewhere
- diag: log NVS free/used entry counts on every boot, and a loud, specific log line if the standard `nvs_flash_init()` boilerplate ever has to erase the entire NVS partition (`ESP_ERR_NVS_NO_FREE_PAGES`/`ESP_ERR_NVS_NEW_VERSION_FOUND`) - added to investigate the WiFi-credential-loss reports above; ruled out as the cause for those specific incidents (NVS had hundreds of free entries at the time) but kept as ongoing low-cost health telemetry
- fix: even with the 3-attempt retry above, a cold boot that happened to hit 3 straight non-rejection failures (confirmed live: `WIFI_REASON_AUTH_EXPIRE`/`WIFI_REASON_CONNECTION_FAIL` during a spell of weak signal, never an actual credential rejection) still unconditionally wiped the saved credentials once those 3 attempts ran out. New opt-in Web UI toggle "Extended WiFi retry before reprovisioning" (General tab, off by default - live-verified worst case is up to ~6x the energy use of the default behavior, so this is opt-in for mains/USB-powered frames rather than a new default for every device): when turned on, a non-rejection failure persists a running attempt count in NVS and retries via a fresh reboot instead, up to 10 total attempts across reboots, before finally falling back to the reprovisioning wipe - a genuine credential rejection is completely unaffected either way and still wipes after a single attempt
- fix: even with the extended-retry option above enabled, a device that kept failing to reconnect for reasons other than a genuine credential rejection (confirmed live: repeated `WIFI_REASON_AUTH_EXPIRE`/`WIFI_REASON_CONNECTION_FAIL` a few meters from a working repeater) still eventually wiped its saved credentials once the extended-retry cap ran out, forcing a full manual reprovision even though the password was correct throughout - and without extended retry enabled, this happened after the very first 3-attempt cold-boot exhaustion. New opt-out Web UI toggle "Reprovision (clear saved WiFi credentials) when connection attempts run out" (General tab, on by default - unchanged existing behavior): when turned off, a non-rejection exhaustion keeps the saved credentials instead of wiping them - if Deep Sleep is enabled the frame goes to sleep until its next scheduled wake and retries the whole connection sequence fresh from there, otherwise it continues the rest of that boot without WiFi (every later network-touching step already tolerates being offline, so this never blocks) and retries fresh on the next cold boot. A genuine credential rejection is completely unaffected and still wipes after a single attempt either way, since a wrong password can't fix itself by waiting
- feat: on-demand offline hotspot (github.com/aitjcize/esp32-photoframe#90) - hold the physical BOOT button for 3 seconds (previously unused past 3s on that key) or use the new "Start offline hotspot" button (General tab) to switch the device into its own WiFi access point (same open SSID scheme as first-time setup) with the *entire* existing web UI (gallery, upload, settings, Agenda config) reachable at `http://192.168.4.1` - no WiFi network, router, or internet involved at all. No server-side changes were needed for this: the main web server was already network-interface-agnostic, so only the underlying WiFi mode switches (`wifi_manager_start_ap_hotspot()`/`_stop_ap_hotspot()`); the auto-sleep timeout is extended for the session so it isn't cut short mid-upload. A long BOOT hold again (or the matching Settings button) reconnects to the saved WiFi network. New first-time-setup checkbox "Use offline - no WiFi network" configures a device for permanent offline use (skips the SSID/password requirement and the cold-boot WiFi connect attempts entirely) for exactly this same hotspot to be the only way to manage it
- feat: opt-in HTTPS for the web UI (github.com/aitjcize/esp32-photoframe#130, off by default) - a second, TLS-encrypted server on port 443 alongside the existing plain-HTTP one on port 80, which keeps working completely unchanged (nothing that already talks to a device over plain HTTP, e.g. Home Assistant, breaks). Each device generates its own ECDSA P-256 self-signed certificate the first time it's enabled and persists it (NVS), rather than shipping one shared key in every firmware image (which would let anyone extract it and impersonate/MITM any device) - browsers show a one-time "not secure" click-through warning as a result, since no CA can vouch for a device with no public hostname; this protects a session from passive LAN sniffing, not from an active attacker who ignores that warning. `main/https_cert.c` is new; the existing ~62 route registrations (gallery, upload, settings, Agenda config, everything) are shared verbatim between both server instances via one extracted registration function - no per-route changes anywhere
- fix: an Agenda wake whose WiFi connection attempt had already failed (e.g. a weak-signal cold boot) still spent ~15 seconds retrying Calendar A/B, ToDo, and weather fetches against a connection already known not to exist, before falling back to local-only content (Calendar C/D/E, already-cached ICS files needing no network) it could have rendered immediately. `agenda_manager_run()` now takes the wake's actual connection result and skips those three network-dependent sources outright when it's false, rather than each independently discovering the same already-known failure
- fix: the external RTC (PCF85063/PCF8563) read back a time exactly one hour ahead of the true time whenever DST was actually in effect — its `read_time()` zero-initialized `struct tm` before calling `mktime()`, which forces `tm_isdst = 0` ("standard time") instead of `-1` ("work it out from the date"), so a value written during CEST (UTC+2, via the already-correct `localtime_r()` in `write_time()`) got read back as if it were CET (UTC+1); self-corrected only by the next SNTP sync, then recurring on every subsequent cold boot for as long as DST stayed active. Confirmed live from a boot's restored wall-clock landing exactly 1h ahead of a manual NTP sync moments later. The same `{0}`-init bug also affected local (non-UTC) Agenda Calendar event times parsed from an `.ics` file — fixed identically

**Agenda Mode** ([colors doc](AGENDA_COLORS.html)):
- feat: ToDo (todo.txt-format URL) and/or Calendar (up to two ICS/iCal URLs, merged and sorted together) content, shown full-screen alone or together (stacked or side-by-side), on its own independent cron schedule that runs alongside normal photo rotation rather than disabling it — a matching wake renders directly to the panel and skips the photo pipeline for that cycle only; normal rotation continues on its own next scheduled wake
- feat: per-element ToDo coloring (priority, `+project`/`@context` tags, due-date urgency, each colored independently) and per-calendar-source coloring (Calendar A/B get their own color); day-grouped Calendar view with a divider per day
- feat: multi-day Calendar event display, selectable in the Web UI (Agenda settings): "Repeat" (default, shown under every day it spans), "Compact" (shown once, on its first visible day, with an "N/M:" span-position prefix), or "Repeat + number" (repeated under every day like "Repeat", but each occurrence also carries the same "N/M:" prefix)
- feat: configurable shared background (white/black/any hardware-supported color) with automatic fallback if a text color would otherwise match it; landscape layout choice between stacked and side-by-side ToDo/Calendar columns
- feat: adjustable Calendar lookahead window (1-3 days, Web UI: Agenda settings)
- feat: full per-role Web UI color picker for every ToDo/Calendar color (Spectra6/color boards)
- feat: opt-in weather forecast annotation on each Calendar day divider (e.g. "Fri 11. [18/25 cloudy]"), reusing the same location/provider settings as the existing photo weather overlay, independent toggle
- fix: a Calendar event's `VALARM` reminder block could overwrite the real event's title if the alarm itself carried its own `SUMMARY`
- fix: TLS fetch failure against calendars whose certificate chain terminates at a cross-signed root (affects Google Calendar's current chain) — enabled cross-signed root verification in the mbedTLS certificate bundle
- fix: a batched Agenda-settings save could race with a concurrent NVS write and drop part of the update; saved as a single atomic batch now
- fix: factory reset erased the Agenda ETag cache validators from NVS but left the matching `.agenda_*_cache.*` files behind on storage - now removed too
- feat: conditional GET (ETag) caching for both the ToDo and Calendar fetches — an unchanged source skips re-downloading on the next agenda wake and re-parses a small on-device cache instead, mirroring the same pattern already used for the URL-rotation image fetch
- fix: Agenda mode only ever fired from the deep-sleep timer-wake path, so it silently never ran at all with Deep Sleep disabled (Home Assistant / always-on use) — the always-on rotation task now runs Agenda on its own independent schedule too, under the same "USB-powered or deep sleep disabled" condition photo rotation already uses there
- feat: three extra ICS calendar sources (Calendar C/D/E) shown in the same Calendar column as A/B, each independently enabled/named/colored, for feeds you don't want auto-refreshed (e.g. a downloaded holidays/school-holidays list) — provide a URL (fetched once, only re-fetched on an explicit "refresh now" or a URL change) or upload a `.ics` file directly; a source that runs out of upcoming events shows a permanent reminder in the calendar naming which one needs an update
- feat: those same three sources parse a large raw `.ics` file at most once every 30 days instead of on every agenda wake — the first read (or a re-fetch/upload) expands it into a small flat cache of individual event instances, and every wake after that just reads the flat cache until it runs dry, then re-expands for the next 30 days
- fix: the RRULE recurrence expander's occurrence-count safety cap was a fixed 8, correct only for the 1-3 day rotation/agenda lookahead windows every caller used at the time it was written — the new 30-day expansion above would have silently truncated a daily-recurring event to its first ~8 days and dropped the rest; the cap is now computed from the actual window width instead of hardcoded
- feat: timed Calendar events can show more than just their bare start time — "Event time display" setting (next to the multi-day display mode) offers Off (default, unchanged), Duration ("08:15 [45m] Kaffee trinken", compact for short events), or Range ("08:15-09:00 Kaffee trinken", more compact once an event runs over an hour); never affects all-day events
- feat: persistent "configured" checkmark on each of the five Calendar sources' URL/upload fields (A/B/C/D/E) — previously a successful save/upload only flashed a temporary toast, so there was no lasting way to tell a working calendar apart from one that was never actually given a source, especially after a page reload
- fix: it was possible to enable any of the five Calendar sources (A/B or C/D/E) with no URL or uploaded file behind it at all — that source would then just silently render nothing forever, with no error anywhere; enabling a source now requires it to actually have a URL/file first (typed this session or already confirmed on the device), while turning one off remains unrestricted
- feat: Calendar C/D/E now get a colored letter badge in the header when they contribute events that cycle (A/B already showed their name + a color swatch) — the letter itself, not just the color, disambiguates the three even on grayscale boards (where every source draws in plain black) and even on color boards, since C and E share the same default color
- feat: opt-in 7-day Calendar grid layout (Settings → Agenda → "Layout"), two selectable templates — a fixed 4-row x 2-column grid where today gets double space (Template A: full-width row; Template B: double-height half-width cell), the other 6 days filling the remaining cells column-major, every day header landing on one of the 4 shared row boundaries regardless of column. Only takes effect when Calendar is shown alone (ToDo off); falls back to the existing 1-3 day list otherwise. Each day cell gets its own independent, fixed-height "+N more" truncation instead of one shared budget across the whole list. The per-day weather forecast annotation now covers all 7 grid days (still 3 in list mode, and unchanged for the plain photo overlay's own weather line)
- feat: optional 2-group rotation/"shift" coloring for the 7-day grid (e.g. a 2-2-3 custody-style schedule) — three presets (2-2-3, week/week, 3-4), each alternating which group starts every 7-day half for the real alternating-fortnightly convention those schedules use, anchored to a configurable start date; colors only the day's appointment lines, never the day header itself (see the fix entry below for why), with two configurable colors
- fix: the header's calendar name(s)/letter badges and timestamp were separated by wider-than-necessary or inconsistent spacing (a "," only between the two named calendars, no separator at all before/between C/D/E's letter badges; " - " before the date) and the date always showed a 4-digit year — every shown source (A/B/C/D/E) now gets a "," before it if anything already precedes it, "-" replaces " - " with no surrounding spaces, and the year shortens to 2 digits, freeing up header width now that C/D/E's badges can also be present
- fix: a Calendar event's text color (resolved once against the page's own background) could go invisible when the 7-day grid's shift-model coloring happened to use the same color as that event's own Calendar source — e.g. green Calendar text on a green shift-colored day; now re-checked against whichever background a given row actually ends up on before drawing. A colored weather icon's fixed traffic-light hue had the identical risk against a shift-colored day header — resolved structurally instead, by never shift-coloring the day header at all (only the appointment lines below it), documented with a generated, verifiably-accurate color matrix in `docs/AGENDA_COLORS.html` (`scripts/generate_agenda_grid_colors_doc.py`)
- fix: each 7-day grid cell's row budget silently wasted up to one row's worth of height at the bottom (the per-row-height formula assumed a trailing gap after every row, including the last one, which doesn't need one) — visible as a blank strip between a lightly-booked day's last line and the next day's header even though its own cell still had unused room; the same off-by-one existed in the plain list view and ToDo column. All three now compute the row budget so the last row's height is used for content instead of an unneeded trailing gap
- fix: the 7-day grid's shift-model coloring only filled the background behind rows that actually had an event, leaving a plain black/white gap wherever a day's cell had fewer events than its fixed row budget; the entire appointment area of a shift-colored day (down to the cell's own bottom edge, including any unused rows and the "+N more" line) is now filled as one continuous block instead
- feat: Calendar view coloring is now driven entirely by a user-imported color-profile JSON instead of a fixed set of per-role hue pickers — import a profile exported from the standalone `profile-editor.html` visual editor (Settings → Agenda → Appearance → "Calendar Color Profiles") to control the day text/background, per-day header, shared top header, each Calendar source's color, and (with the rotation pattern above) the single color that marks a day; up to 3 profiles can be stored on the device (`POST`/`DELETE /api/agenda/color-profile?slot=1|2|3`, `GET` lists names + which is active), with exactly one active at a time and no code-level limit on re-importing/replacing a slot. Removes the old `agenda_bg_color`/`agenda_cal_a..e_color`/`agenda_shift_color1`/`agenda_shift_color2` settings, which the profile now fully supersedes (the ToDo column keeps a fixed plain black-on-white page and its own independent per-role color pickers, unaffected by this). A profile's `markColorsHeader` field can now genuinely recolor the day header when marked (reversing the previous hardcoded "header never colored" rule) since that choice now comes from the imported profile rather than a fixed device setting. On a grayscale/monochrome display, a color-mode profile is automatically shown as a plain black/white inversion instead of its authored hues, chosen from whether the profile's own page background leans light or dark, rather than requiring a separate mono-authored profile
- feat: Calendar C/D/E's header identifier (and A/B's name) is now plain text colored in that source's own profile color, matching `profile-editor.html`'s own header legend, instead of a filled colored box with a contrast-derived letter
- feat: the `profile-editor.html` visual editor tool is now served directly by the device itself at `GET /profile-editor.html` (Settings → Agenda → Appearance → "Open Color Profile Editor") and gained an "An Gerät senden" (send to device) control that `POST`s the currently-edited profile straight to a chosen device slot via the endpoint above — no manual export-then-import round trip needed when editing on the same network as the device
- fix: a truncated Calendar event/ToDo line (and Telegram caption) used a 3-character "..." marker and only ever kept whole words, so a long summary that didn't fit as a full extra word was dropped entirely even when a few more characters would have fit (e.g. a "Fahrradwerkstatt 17 - 20 Uhr" appointment showed as "10:00 [1h]...", the entire summary gone) — the marker is now a single "~" (Font24's own last printable glyph; a real ellipsis character has no bitmap glyph in this font), and the truncated line now fills any leftover room with as many leading characters of the next word as fit before the marker, e.g. "10:00 [1h] Fahrradw~"
- feat: new color-profile option `headerDividerFilled` (profile-editor.html: "Trennlinie im Tagesheader ohne Lücken") — the per-day header's dashed separator normally leaves the page background showing through its gaps between dashes, which could visually read as two different colors within one header even though the dashes and the day label use the identical header bg/ink pair; enabling it fills the whole divider row as one continuous block instead
- fix: the weather forecast chip's icon shared the day header's own background (`[13/19 <icon>]`, icon drawn inside the bracket) - since the icon's color is a fixed, non-configurable traffic-light indicator of the weather condition, a same-colored header (including either of a shift model's two states, on both color and monochrome displays) could render it invisible. The icon now gets its own independent background, drawn as a separate chip right after the bracket instead of inside it (`[13/19] <icon>`) — configurable per color profile (`iconBg`, profile-editor.html: "Wetter-Icon Hintergrund") in color mode; on a monochrome profile the icon background is instead always the exact opposite of that specific day's own resolved header color, a structural guarantee against collision since only two colors exist there. Respects the existing "Right-align forecast" placement setting either way
- fix: the Calendar/ToDo top header's timestamp was left-aligned immediately after the header's own content (calendar names/badges, or "TODO"), preceded by a dash, with a 4-digit year on the ToDo side (the Calendar side already used 2 digits) — now right-aligned in both headers, immediately left of the climate readout when one is shown, with no dash and a 2-digit year on both sides. Confirmed live in the side-by-side dual layout's half-width Calendar column that several long calendar names/badges can leave no room for the timestamp at all - it's now omitted entirely in that case rather than overlapping/garbling into the header's own content
- fix: the optional climate/sensor readout (`climate_agenda_header_enabled`) appeared in both headers when ToDo and Calendar were shown together, duplicating the same reading twice on one screen — now shown in only one header: the top one (ToDo) when stacked, or Calendar's (the right column) when side-by-side, so it always lands in the display's actual top-right corner exactly once
- fix: a color profile's shift-model marking (the "mark" color/`markColorsHeader`) only ever appeared in the 7-day grid layout (Calendar shown alone, full-screen) and silently reverted to the profile's plain unmarked colors the instant ToDo was shown alongside it (stacked or side-by-side), even on the exact same day and the same active profile — the List layout's per-day header now resolves the shift model the same way the grid does (body marking still has no List-mode equivalent, since List has no per-day cell area to fill solidly)
- fix: a mono (monochrome-target) color profile's shared top header and per-day header both used the same black/white polarity as the day body/page, instead of the opposite polarity `profile-editor.html`'s own mono model actually specifies for both of those elements (its `mono()` always makes a header's bg/ink the inverse of the page's own bg/ink) — confirmed live: a `mono-invert` profile's top header rendered black-on-white (matching the page) instead of the correct white-on-black
- feat: the color profile's `headerFollowsEntries`/`headerFollowsEntriesColor` switches (mono/color mode respectively) are now implemented — when on, an unmarked day's header abandons its own header color pair and shows the same colors as the day's body instead, exactly mirroring `profile-editor.html`'s own per-day header logic (including mono's inherent quirk, inherited directly from the tool's math, where a *marked* header always ends up identical to its own default color regardless of which switch triggered the marking — mono only has two colors to work with, and marking already picks the same one both switches would)
- feat: the color profile's `dayheadLeaderLine` switch (a decorative dashed rule between the day label and its weather forecast, only relevant when weather annotations are on) is now implemented, drawn in the header's own ink color exactly like `profile-editor.html`'s live preview (`border-bottom:2px dashed currentColor`) — previously parsed but silently ignored, so the switch had no visible effect regardless of its value
- fix: a mono/mono-invert color profile's day-header weather icons still rendered in their full "colored" traffic-light hues (a blue cloud, a yellow sun, etc.) instead of plain black/white, since that coloring was only ever gated on the device-wide "colored weather icons" setting and the panel's own hardware grayscale capability — neither of which knows about a profile-level choice to render everything in two colors on an otherwise full-color Spectra6 panel. Confirmed live via a mono-invert profile whose day-header chips stayed fully colored on-device despite every other element correctly rendering in black/white
- fix: a day header whose own resolved background color diverged from the Calendar column's single ambient page fill (most commonly a marked header, e.g. white, sitting in an otherwise black/unmarked column) showed a thin colored frame of that ambient color around it — `headerDividerFilled`'s solid-fill background only ever painted an inset sub-rectangle (`total_w`/`FONT_HEIGHT`, matching the label/dash positioning math), never the row's full gutter-to-gutter width and full row height, leaving its own outer padding margins unfilled and showing whatever was painted underneath beforehand instead. Now fills the complete row (matching the top header's own already-gutter-to-gutter fill) — confirmed live via a marked header's frame disappearing once the fix was flashed
- feat: color profiles can now choose the weather icon's background separately for a marked vs. an unmarked day (`iconBgMarked`, profile-editor.html: "Wetter-Icon Hintergrund (markiert)") — previously `iconBg` was one literal value applied regardless of marking, so a profile that changes header/body colors when a day is marked couldn't also give the icon a matching independent color for that state. Defaults to `iconBg`'s own value when unset, so an existing profile renders identically until this new field is deliberately set; mono profiles are unaffected either way (icon bg there is always the structural opposite of that day's own header color)
- feat: a device with no color profile slot activated now renders Agenda with a real, fully-styled compiled-in default (`main/resources/default_agenda_color_profile.json`, originally exported as "s2_BETA") instead of the previous bare black/white/no-switches fallback — reached through the exact same parser as an uploaded profile, so it stays in sync with every field this doc describes; still just a firmware default, overridden the moment any slot is activated
- fix: with a color profile's `headerFollowsEntriesColor` switch on, a *marked* day's header took the mark color as its background but the day's plain body text color verbatim as its ink, with no contrast check at all (unlike `markColorsHeader`'s own marked-header ink, which is auto-derived for contrast) — a profile whose mark color and body text color happened to match (e.g. both "white") rendered that header's date/weekday completely invisible, confirmed live with exactly that combination. New opt-in profile switch `headerFollowsEntriesColorSafeInk` ("Markierter Header: Text automatisch kontrastsicher") replaces that literal ink with an auto-derived black/white contrast against the mark color, scoped to only the header's ink — unlike turning on `markColorsHeader` instead (the only pre-existing workaround), which also stops the mark color from applying to the day's body at all

**Chimes** (`waveshare_photopainter_73` only — onboard ES8311 codec + NS4150B speaker, previously unused):
- feat: short, synthesized beep feedback (no WAV/melody files) for seven firmware events — photo rotated/refreshed, a new photo received via Telegram, low battery warning, WiFi reprovisioning about to wipe credentials, an overdue/due-today Agenda ToDo item, a successful firmware update, and WiFi/internet lost several wakes in a row — each individually enabled from a new **Chimes** settings tab (hidden entirely on boards without this speaker)
- feat: a 3-way speaker mode — Off (default), Battery + mains, or Mains/USB only (recommended for battery frames, since the amplifier draws noticeable current) — plus an optional daily quiet-hours window (wraps past midnight) that silences every event regardless of mode
- feat: "Play test tone" button plays immediately, ignoring quiet hours and the speaker mode, to confirm the hardware/wiring works right after enabling
- fix (bring-up): the ES8311 codec accepts and acknowledges every register write regardless of whether the resulting configuration actually produces sound — two internal register-sequence attempts based on a generic/simplified bring-up left several registers unset and used a reset sequence not present in the working reference, producing a fully "successful" but silent codec; live-traced against Waveshare's own shipped firmware to get a byte-for-byte correct sequence
- fix (bring-up): even with a correct register sequence, a short (~150ms) beep stayed inaudible while a long test tone was fine on the same hardware — the NS4150B amp's own soft-start ramp was eating the whole beep before it finished settling; raised the post-PA-enable delay from 30ms to 250ms
- feat: a 0-100% volume slider (Web UI + `PATCH /api/config`'s `chime_volume`), linearly mapped to the ES8311's DAC volume register, applied equally to every event/kind — no separate loud override for warning/error tones
- feat: the three "actionable" events (low battery, Agenda due/overdue, WiFi/internet critical error) now repeat their chime once per check while the underlying condition stays true, up to 5 times, then go quiet until it's resolved (at which point a later recurrence repeats again from the start) — previously each fired only once (or once per day for Agenda), which could go unnoticed if missed. The other four events remain one-shot. The generic per-boot anti-loop safety cap is unified to the same limit (5)

**Climate** (any board whose onboard SHTC3 temperature/humidity sensor answers — the sensor driver and `board_hal_get_temperature()`/`get_humidity()` already existed for several boards including `waveshare_photopainter_73`, just with no Web UI, history, or classification built on top of them):
- feat: a new **Climate** settings tab (hidden on boards without a responding sensor) lets you pick one of 5 preset room types (Living Room/Office, Bedroom, Bathroom, Kitchen, Basement) and a display unit (Celsius default, or Fahrenheit); each reading is independently classified as Bad (mold/dryness risk), Good, or Super (optimal) for temperature and humidity separately — the two are never merged into one combined verdict
- feat: an optional top-right badge pair on every photo showing the latest temperature and humidity, background-colored by category (Red/Yellow/Green on color boards — Yellow stands in for orange, this hardware has no true orange; a single black badge with no color distinction on grayscale-only boards); off by default
- feat: the same readout, right-aligned, optionally shown in both column headers (ToDo and Calendar) in Agenda mode; off by default
- feat: a new **Climate History** chart (alongside the existing Battery History tab) logging one reading per successfully displayed image, with a manual reset button — logging is on by default and has no effect on what's displayed
- feat: new `GET/DELETE /api/climate-history` endpoints and `climate_room_type`/`climate_temp_unit`/`climate_logging_enabled`/`climate_overlay_enabled`/`climate_agenda_header_enabled`/`climate_sensor_available` fields on `GET`/`PATCH /api/config`
- feat: a reading is now logged on every wake (deep-sleep timer/rotate-button/clear-button, plus every ~6 minutes while the device stays awake continuously) instead of only when an image is actually displayed, so the history stays meaningful even on wakes that don't rotate/render anything — throttled to at most once every 5 minutes regardless of which trigger fires, via a debounce that survives deep sleep
- feat: a user-settable calibration offset (°C/°F and percentage points, default 0) is applied to every displayed/logged climate value, for hardware that reads consistently high/low — the raw, un-offset reading stays available via the existing `/api/sensor` diagnostic endpoint for reference
- feat: the Climate History chart now shows Y-axis gridlines/labels (in the selected unit) on both the temperature and humidity mini-charts, matching the existing Battery History chart's convention
- fix: the top-right climate overlay badges could visually overwrite the tail of the weather/headlines(RSS) bar instead of leaving room for it — the bar now reserves space and truncates with "…" ahead of the badges instead
- fix: `config_manager`'s `climate_last_log_time` used a 16-character NVS key (`"climate_last_log"`), one over NVS's hard 15-character limit — the debounce silently never persisted across a reboot, only across an already-running session (renamed to `"climate_lastlog"`)
- fix: `http_server_init()`'s `max_uri_handlers` margin (55) was exactly consumed by this feature's own new endpoints, silently dropping the last-registered handler with only a one-line boot warning (`no slots left for registering handler`) and no other symptom — raised to 64
- fix: both the photo-overlay climate badges and the Agenda-header climate chip always drew white text, illegible on the Good category's Yellow background on the actual e-paper panel (fine on Red/Green/the grayscale-only Black) — black text now used specifically for that one background

**Reliability & infrastructure**:
- feat: DNS backup/fallback servers (Cloudflare `1.1.1.1`, Google `8.8.8.8`) now populate lwIP's built-in multi-server fallback slots, previously left empty — a single flaky or unreachable DNS server (typically the router's own, handed out via DHCP) could otherwise fail to resolve *any* hostname (weather, Telegram, headlines alike) for a whole wake cycle with no automatic recovery
- feat: "no internet" error banner — tracks consecutive wake cycles where WiFi connected fine but every internet-dependent request (weather, headlines, Telegram, Home Assistant, URL-mode fetch) still failed anyway (e.g. a transient DNS outage); shares the same threshold/toggle/counter as the existing WiFi-connect-failure banner, since both are just flavors of "no usable internet this wake"
- fix: debug-log flush ran *after* the SD card was unmounted during deep-sleep entry, racing the log writer task against the unmount and crashing on the next boot's flush attempt — reordered to flush first
- fix: OTA update-check parsed GitHub's release API response assuming a fixed `Content-Length`, which fails against GitHub's actual chunked-transfer responses (`"Invalid content length: 0"`) — switched to the existing streaming HTTP client already used elsewhere
- fix: OTA/update-check pointed at upstream's GitHub repo instead of this fork's — an automatic update would have silently replaced this fork's firmware with vanilla upstream
- fix: `PATCH /api/config` silently rejected (HTTP 400, no other symptom) any request body over 4096 bytes — a full Settings-page config export/import round trip (device name, timezone, WiFi, Agenda, Chimes, Climate, overlays, etc. all in one request) is comfortably larger than that on a device with several Agenda sources/settings configured, so re-importing a previously exported config onto another device could silently apply nothing at all beyond whatever the Web UI split into smaller separate requests (e.g. the color palette). Raised to 32768 bytes; the buffer is also now allocated from PSRAM instead of the internal heap
- fix: `apply_config_from_json()` (the shared handler behind `PATCH /api/config`, used by both the Settings page and the config-import flow) processed its ~150 settings fields sequentially but stopped dead — silently discarding every field after the one it was on — the moment any single field failed validation (an unreachable WiFi SSID, a non-numeric Telegram chat ID, an invalid cron expression, a bad static-IP/gateway/DNS address, an HTTPS cert-pinning failure), even though every field is otherwise independent. Confirmed live: a real config export re-imported onto a second device applied only the fields processed before whichever one first failed, silently dropping Agenda, Chimes, Climate, and Overlay settings that came later in the same request with nothing wrong with them (this is also what "Show upcoming events" wouldn't turn on turned out to be — it's just an ordinary Agenda field caught in the same fallout). Every one of the 19 places this could happen now validates and reports its own field's error without preventing any other field in the same request from applying; the endpoint's response still reports the single most recent validation failure, so a genuinely bad field is never silently accepted, just no longer able to take the rest of the request down with it
- fix (webapp): the Settings page's config-import routine reported "Import successful" even when one or more of its several parallel requests (config/processing/palette/per-album) came back with an HTTP error status — `fetch()` only rejects on a network failure, not an HTTP error code, so an unchecked `Promise.all()` around them treated a 400/500 response exactly like success. Each request's response status is now checked explicitly, and a partial failure reports specifically which part(s) didn't apply and why, instead of a blanket false "successful"
- fix: `PUT /api/albums/enabled` reported a generic HTTP 500 ("Failed to update album") when asked to enable an album whose folder doesn't exist on that specific device yet (album folders are created by uploading a photo into them, not by config) — surfaced directly by the config-import fix above once import failures actually started being reported: importing a config exported from a device with a differently-named/larger photo library onto another device reads as a firmware bug ("HTTP 500") rather than the expected "this device has no photos in that album" condition it actually is. Now reports `404 Not Found` with an explanatory message instead; disabling a nonexistent album is unaffected either way (already always succeeded, by design)
- fix (webapp): on a device with no Calendar A/B ever configured (a fresh/factory-reset device, no config import), the "Show upcoming events" switch could never be turned on at all - enabling it requires a Calendar A or B URL to already be present, but the URL input fields were themselves disabled until the switch was already on, a deadlock with no way out from the UI. The Calendar A/B URL/display-name/days-ahead fields are no longer gated by the switch they're needed to unlock; a caption next to the switch now explains the "enter a URL first, then enable" order
- feat: own CI/release pipeline — 7-board build matrix, automatic draft releases with per-board flashable binaries, GitHub Pages web flasher kept in sync with the latest published release
- feat: `v218.x.y` versioning — the major version permanently encodes the upstream base release (`v2.18.0`) this fork tracks; minor/patch are this fork's own release counter